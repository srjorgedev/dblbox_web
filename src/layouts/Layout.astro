---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Analytics from "@vercel/analytics/astro";

interface Props {
	Title: string;
	ICO: string;
	Cannonical: URL;
}

const { Title, ICO, Cannonical } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link
			rel="icon"
			type="image/*"
			href={ICO == "default" ? "https://raw.githubusercontent.com/srjorgedev/dblbox_resources/refs/heads/main/bchaico/539.webp" : ICO}
		/>
		<meta name="generator" content={Astro.generator} />
		<title>{Title}</title>
		<meta name="google-adsense-account" content="ca-pub-7780837724792452" />
		<base href="https://www.dbl-box.com/" />
		<meta property="og:title" content={`${Title} | DBL BOX`} />
		<meta name="twitter:title" content={`${Title} | DBL BOX`} />
		<link rel="canonical" href={Cannonical} />
		<meta
			name="description"
			content="DBL BOX es tu fuente definitiva para todo sobre Dragon Ball Legends. Encuentra información detallada de personajes, habilidades, equipamientos y más, todo presentado de manera clara y accesible. Además, disfruta de herramientas como una calculadora de rangos y un Tiermaker para clasificar a tus personajes favoritos de forma fácil y divertida."
		/>
		<meta
			name="keywords"
			content="DBL BOX, Dragon Ball Legends, DB Legends, Dragon Ball, DB Legends, Dragon Ball Legends API, Characters Dragon Ball Legends, Rank Calculator, Tiermaker DBL, BOX DBL"
		/>
		<link rel="sitemap" href="/sitemap.xml" />
		<script
			is:inline
			async
			src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7780837724792452"
			crossorigin="anonymous"></script>
	</head>
	<body>
		<main class="layout">
			<Header />
			<slot />
			<Analytics />
			<Footer />
		</main>
	</body>
</html>

<style is:global>
	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
		border: none;
	}

	:root {
		--bg-main: rgb(10, 22, 37);
	}

	html {
		color: #f1f1f1;
		font-size: 12px;
		font-family: Arial, Helvetica, sans-serif;
	}

	html,
	body {
		width: 100%;
		background-color: var(--bg-main);
		scrollbar-gutter: auto;
		font-size: 1.15rem;
	}

	main.layout {
		height: 100%;
		margin: 0 auto;
		position: relative;
		width: 100%;

		display: flex;
		flex-direction: column;
		min-height: 100vh;
	}

	@media (max-width: 769px) {
		main.layout {
			max-width: 100%;
		}
	}

	@media (min-width: 769px) {
		main.layout {
			max-width: calc(100% - 2rem);
		}
	}

	@media (min-width: 1080px) {
		main.layout {
			max-width: 1024px;
		}
	}

	@media (min-width: 1440px) {
		main.layout {
			max-width: 1280px;
		}
	}
</style>
